id: functor_decl
message: Add generation to first
severity: warning # error, warning, info, hint
language: sml
rule:
  pattern: $BRANCH
  kind: fctbind 
  
fix: (* GEN BEGIN FUNCTOR DECL *) $BRANCH (* GEN END FUNCTOR DECL *)

---

id: function_exp
message: Add generation to first
severity: warning # error, warning, info, hint
language: sml
rule:
  pattern: $BRANCH
  kind: fn_exp 
  
fix: (* GEN BEGIN FUNCTION EXPRESSION *) $BRANCH (* GEN END FUNCTION EXPRESSION *)

--- 

id: tag_inside_let 
message: Add generation to first
severity: warning # error, warning, info, hint
language: sml
rule: 
  pattern: $BRANCH
  any: 
    - kind: val_dec
    - kind: fun_dec

fix: (* GEN BEGIN TAG INSIDE LET *) $BRANCH (* GEN END TAG INSIDE LET *)

--- 
id: signature_decl
message: Add generation to first
severity: warning # error, warning, info, hint
language: sml
rule:
  pattern: $BRANCH
  kind: signature_sigdec 
fix: (* GEN BEGIN SIGNATURE DECLARATION *) $BRANCH (* GEN END SIGNATURE DECLARATION *)