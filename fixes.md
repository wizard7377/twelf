Outside of `./lib/compat/array.ml` change each occurence of `COMPAT_ARRAY` to `Array.COMPAT_ARRAY`
Outside of `./lib/compat/array_slice.ml` change each occurence of `ARRAY_SLICE` to `Array_slice.ARRAY_SLICE`
Outside of `./lib/compat/compat.ml` change each occurence of `COMPAT` to `Compat.COMPAT`
Outside of `./lib/compat/mono_array_slice.ml` change each occurence of `MONO_ARRAY_SLICE` to `Mono_array_slice.MONO_ARRAY_SLICE`
Outside of `./lib/compat/path.ml` change each occurence of `COMPAT_PATH` to `Path.COMPAT_PATH`
Outside of `./lib/compat/socket.ml` change each occurence of `COMPAT_SOCKET_IO` to `Socket.COMPAT_SOCKET_IO`
Outside of `./lib/compat/substring.ml` change each occurence of `COMPAT_SUBSTRING` to `Substring.COMPAT_SUBSTRING`
Outside of `./lib/compat/text_io.ml` change each occurence of `COMPAT_TEXT_IO` to `Text_io.COMPAT_TEXT_IO`
Outside of `./lib/compat/time_limit.ml` change each occurence of `TIME_LIMIT` to `Time_limit.TIME_LIMIT`
Outside of `./lib/compat/timer.ml` change each occurence of `COMPAT_TIMER` to `Timer.COMPAT_TIMER`
Outside of `./lib/compat/vector.ml` change each occurence of `COMPAT_VECTOR` to `Vector.COMPAT_VECTOR`
Outside of `./lib/compat/vector_slice.ml` change each occurence of `VECTOR_SLICE` to `Vector_slice.VECTOR_SLICE`
Outside of `./lib/compat/word8_vector_slice.ml` change each occurence of `MONO_VECTOR_SLICE` to `Word8_vector_slice.MONO_VECTOR_SLICE`
Outside of `./lib/compat/word8_vector_slice.ml` change each occurence of `COMPAT_WORD8_VECTOR_SLICE` to `Word8_vector_slice.COMPAT_WORD8_VECTOR_SLICE`
Outside of `./lib/compile/assign.ml` change each occurence of `ASSIGN` to `Assign.ASSIGN`
Outside of `./lib/compile/compile.ml` change each occurence of `COMPILE` to `Compile.COMPILE`
Outside of `./lib/compile/compsyn.ml` change each occurence of `COMPSYN` to `Compsyn.COMPSYN`
Outside of `./lib/compile/cprint.ml` change each occurence of `CPRINT` to `Cprint.CPRINT`
Outside of `./lib/compile/subtree.ml` change each occurence of `SUBTREE` to `Subtree.SUBTREE`
Outside of `./lib/compress/compress.ml` change each occurence of `COMPRESS` to `Compress.COMPRESS`
Outside of `./lib/compress/sgn.ml` change each occurence of `SGN` to `Sgn.SGN`
Outside of `./lib/cover/cover.ml` change each occurence of `COVER` to `Cover.COVER`
Outside of `./lib/cover/total.ml` change each occurence of `TOTAL` to `Total.TOTAL`
Outside of `./lib/delphin/delphin.ml` change each occurence of `DELPHIN` to `Delphin.DELPHIN`
Outside of `./lib/delphin/dextsyn.ml` change each occurence of `DEXTSYN` to `Dextsyn.DEXTSYN`
Outside of `./lib/delphin/interface.ml` change each occurence of `INTERFACE` to `Interface.INTERFACE`
Outside of `./lib/delphin/parse_prg.ml` change each occurence of `PARSE` to `Parse_prg.PARSE`
Outside of `./lib/delphin/trans.ml` change each occurence of `TRANS` to `Trans.TRANS`
Outside of `./lib/domains/field.ml` change each occurence of `FIELD` to `Field.FIELD`
Outside of `./lib/domains/integers.ml` change each occurence of `INTEGERS` to `Integers.INTEGERS`
Outside of `./lib/domains/ordered_field.ml` change each occurence of `ORDERED_FIELD` to `Ordered_field.ORDERED_FIELD`
Outside of `./lib/domains/rationals.ml` change each occurence of `RATIONALS` to `Rationals.RATIONALS`
Outside of `./lib/flit/flit.ml` change each occurence of `FLIT` to `Flit.FLIT`
Outside of `./lib/formatter/formatter.ml` change each occurence of `FORMATTER` to `Formatter.FORMATTER`
Outside of `./lib/frontend/fquery.ml` change each occurence of `FQUERY` to `Fquery.FQUERY`
Outside of `./lib/frontend/lexer.ml` change each occurence of `LEXER` to `Lexer.LEXER`
Outside of `./lib/frontend/parse_condec.ml` change each occurence of `PARSE_CONDEC` to `Parse_condec.PARSE_CONDEC`
Outside of `./lib/frontend/parse_fixity.ml` change each occurence of `PARSE_FIXITY` to `Parse_fixity.PARSE_FIXITY`
Outside of `./lib/frontend/parse_mode.ml` change each occurence of `PARSE_MODE` to `Parse_mode.PARSE_MODE`
Outside of `./lib/frontend/parse_module.ml` change each occurence of `PARSE_MODULE` to `Parse_module.PARSE_MODULE`
Outside of `./lib/frontend/parse_query.ml` change each occurence of `PARSE_QUERY` to `Parse_query.PARSE_QUERY`
Outside of `./lib/frontend/parse_term.ml` change each occurence of `PARSE_TERM` to `Parse_term.PARSE_TERM`
Outside of `./lib/frontend/parse_thm.ml` change each occurence of `PARSE_THM` to `Parse_thm.PARSE_THM`
Outside of `./lib/frontend/parser.ml` change each occurence of `PARSER` to `Parser.PARSER`
Outside of `./lib/frontend/parsing.ml` change each occurence of `PARSING` to `Parsing.PARSING`
Outside of `./lib/frontend/recon_condec.ml` change each occurence of `EXTCONDEC` to `Recon_condec.EXTCONDEC`
Outside of `./lib/frontend/recon_condec.ml` change each occurence of `RECON_CONDEC` to `Recon_condec.RECON_CONDEC`
Outside of `./lib/frontend/recon_mode.ml` change each occurence of `EXTMODES` to `Recon_mode.EXTMODES`
Outside of `./lib/frontend/recon_mode.ml` change each occurence of `RECON_MODE` to `Recon_mode.RECON_MODE`
Outside of `./lib/frontend/recon_module.ml` change each occurence of `MODEXTSYN` to `Recon_module.MODEXTSYN`
Outside of `./lib/frontend/recon_module.ml` change each occurence of `RECON_MODULE` to `Recon_module.RECON_MODULE`
Outside of `./lib/frontend/recon_query.ml` change each occurence of `EXTQUERY` to `Recon_query.EXTQUERY`
Outside of `./lib/frontend/recon_query.ml` change each occurence of `RECON_QUERY` to `Recon_query.RECON_QUERY`
Outside of `./lib/frontend/recon_term.ml` change each occurence of `EXTSYN` to `Recon_term.EXTSYN`
Outside of `./lib/frontend/recon_term.ml` change each occurence of `RECON_TERM` to `Recon_term.RECON_TERM`
Outside of `./lib/frontend/recon_thm.ml` change each occurence of `THMEXTSYN` to `Recon_thm.THMEXTSYN`
Outside of `./lib/frontend/recon_thm.ml` change each occurence of `RECON_THM` to `Recon_thm.RECON_THM`
Outside of `./lib/frontend/solve.ml` change each occurence of `SOLVE` to `Solve.SOLVE`
Outside of `./lib/frontend/twelf.ml` change each occurence of `TWELF` to `Twelf.TWELF`
Outside of `./lib/frontend/unknownexn.ml` change each occurence of `UNKNOWN_EXN` to `Unknownexn.UNKNOWN_EXN`
Outside of `./lib/global/global.ml` change each occurence of `GLOBAL` to `Global.GLOBAL`
Outside of `./lib/heuristic/heuristic.ml` change each occurence of `HEURISTIC` to `Heuristic.HEURISTIC`
Outside of `./lib/index/index.ml` change each occurence of `INDEX` to `Index.INDEX`
Outside of `./lib/int_inf/int_inf_sig.ml` change each occurence of `INT_INF` to `Int_inf_sig.INT_INF`
Outside of `./lib/lambda/abstract.ml` change each occurence of `ABSTRACT` to `Abstract.ABSTRACT`
Outside of `./lib/lambda/approx.ml` change each occurence of `APPROX` to `Approx.APPROX`
Outside of `./lib/lambda/constraints.ml` change each occurence of `CONSTRAINTS` to `Constraints.CONSTRAINTS`
Outside of `./lib/lambda/conv.ml` change each occurence of `CONV` to `Conv.CONV`
Outside of `./lib/lambda/fgnopn.ml` change each occurence of `FGN_OPN` to `Fgnopn.FGN_OPN`
Outside of `./lib/lambda/intsyn.ml` change each occurence of `INTSYN` to `Intsyn.INTSYN`
Outside of `./lib/lambda/match.ml` change each occurence of `MATCH` to `Match.MATCH`
Outside of `./lib/lambda/normalize.ml` change each occurence of `NORMALIZE` to `Normalize.NORMALIZE`
Outside of `./lib/lambda/order.ml` change each occurence of `ORDER` to `Order.ORDER`
Outside of `./lib/lambda/tomega.ml` change each occurence of `TOMEGA` to `Tomega.TOMEGA`
Outside of `./lib/lambda/unify.ml` change each occurence of `UNIFY` to `Unify.UNIFY`
Outside of `./lib/lambda/whnf.ml` change each occurence of `WHNF` to `Whnf.WHNF`
Outside of `./lib/m2/filling.ml` change each occurence of `FILLING` to `Filling.FILLING`
Outside of `./lib/m2/init.ml` change each occurence of `INIT` to `Init.INIT`
Outside of `./lib/m2/lemma.ml` change each occurence of `LEMMA` to `Lemma.LEMMA`
Outside of `./lib/m2/meta_abstract.ml` change each occurence of `METAABSTRACT` to `Meta_abstract.METAABSTRACT`
Outside of `./lib/m2/meta_global.ml` change each occurence of `METAGLOBAL` to `Meta_global.METAGLOBAL`
Outside of `./lib/m2/meta_print.ml` change each occurence of `METAPRINT` to `Meta_print.METAPRINT`
Outside of `./lib/m2/metasyn.ml` change each occurence of `METASYN` to `Metasyn.METASYN`
Outside of `./lib/m2/mpi.ml` change each occurence of `MPI` to `Mpi.MPI`
Outside of `./lib/m2/prover.ml` change each occurence of `PROVER` to `Prover.PROVER`
Outside of `./lib/m2/qed.ml` change each occurence of `QED` to `Qed.QED`
Outside of `./lib/m2/recursion.ml` change each occurence of `RECURSION` to `Recursion.RECURSION`
Outside of `./lib/m2/search.ml` change each occurence of `OLDSEARCH` to `Search.OLDSEARCH`
Outside of `./lib/m2/skolem.ml` change each occurence of `SKOLEM` to `Skolem.SKOLEM`
Outside of `./lib/m2/splitting.ml` change each occurence of `SPLITTING` to `Splitting.SPLITTING`
Outside of `./lib/m2/strategy.ml` change each occurence of `STRATEGY` to `Strategy.STRATEGY`
Outside of `./lib/meta/abstract.ml` change each occurence of `MTPABSTRACT` to `Abstract.MTPABSTRACT`
Outside of `./lib/meta/data.ml` change each occurence of `MTPDATA` to `Data.MTPDATA`
Outside of `./lib/meta/filling.ml` change each occurence of `MTPFILLING` to `Filling.MTPFILLING`
Outside of `./lib/meta/funnames.ml` change each occurence of `FUNNAMES` to `Funnames.FUNNAMES`
Outside of `./lib/meta/funprint.ml` change each occurence of `FUNPRINT` to `Funprint.FUNPRINT`
Outside of `./lib/meta/funsyn.ml` change each occurence of `FUNSYN` to `Funsyn.FUNSYN`
Outside of `./lib/meta/funtypecheck.ml` change each occurence of `FUNTYPECHECK` to `Funtypecheck.FUNTYPECHECK`
Outside of `./lib/meta/funweaken.ml` change each occurence of `FUNWEAKEN` to `Funweaken.FUNWEAKEN`
Outside of `./lib/meta/global.ml` change each occurence of `MTPGLOBAL` to `Global.MTPGLOBAL`
Outside of `./lib/meta/inference.ml` change each occurence of `INFERENCE` to `Inference.INFERENCE`
Outside of `./lib/meta/init.ml` change each occurence of `MTPINIT` to `Init.MTPINIT`
Outside of `./lib/meta/interpret.ml` change each occurence of `Interpreter` to `Interpret.Interpreter`
Outside of `./lib/meta/mpi.ml` change each occurence of `MTPI` to `Mpi.MTPI`
Outside of `./lib/meta/print.ml` change each occurence of `MTPRINT` to `Print.MTPRINT`
Outside of `./lib/meta/prover.ml` change each occurence of `MTPROVER` to `Prover.MTPROVER`
Outside of `./lib/meta/recursion.ml` change each occurence of `MTPRECURSION` to `Recursion.MTPRECURSION`
Outside of `./lib/meta/relfun.ml` change each occurence of `RELFUN` to `Relfun.RELFUN`
Outside of `./lib/meta/search.ml` change each occurence of `MTPSEARCH` to `Search.MTPSEARCH`
Outside of `./lib/meta/splitting.ml` change each occurence of `MTPSPLITTING` to `Splitting.MTPSPLITTING`
Outside of `./lib/meta/statesyn.ml` change each occurence of `STATESYN` to `Statesyn.STATESYN`
Outside of `./lib/meta/strategy.ml` change each occurence of `MTPSTRATEGY` to `Strategy.MTPSTRATEGY`
Outside of `./lib/meta/uniquesearch.ml` change each occurence of `UNIQUESEARCH` to `Uniquesearch.UNIQUESEARCH`
Outside of `./lib/meta/weaken.ml` change each occurence of `WEAKEN` to `Weaken.WEAKEN`
Outside of `./lib/modes/modecheck.ml` change each occurence of `MODECHECK` to `Modecheck.MODECHECK`
Outside of `./lib/modes/modedec.ml` change each occurence of `MODEDEC` to `Modedec.MODEDEC`
Outside of `./lib/modes/modeprint.ml` change each occurence of `MODEPRINT` to `Modeprint.MODEPRINT`
Outside of `./lib/modes/modesyn.ml` change each occurence of `MODESYN` to `Modesyn.MODESYN`
Outside of `./lib/modes/modetable.ml` change each occurence of `MODETABLE` to `Modetable.MODETABLE`
Outside of `./lib/modules/modsyn.ml` change each occurence of `MODSYN` to `Modsyn.MODSYN`
Outside of `./lib/msg/msg.ml` change each occurence of `MSG` to `Msg.MSG`
Outside of `./lib/names/names.ml` change each occurence of `FIXITY` to `Names.FIXITY`
Outside of `./lib/names/names.ml` change each occurence of `NAMES` to `Names.NAMES`
Outside of `./lib/netserver/netserver.ml` change each occurence of `NETSERVER` to `Netserver.NETSERVER`
Outside of `./lib/opsem/absmachine.ml` change each occurence of `ABSMACHINE` to `Absmachine.ABSMACHINE`
Outside of `./lib/opsem/absmachine_sbt.ml` change each occurence of `ABSMACHINESBT` to `Absmachine_sbt.ABSMACHINESBT`
Outside of `./lib/opsem/abstract.ml` change each occurence of `ABSTRACTTABLED` to `Abstract.ABSTRACTTABLED`
Outside of `./lib/opsem/index.ml` change each occurence of `TABLEINDEX` to `Index.TABLEINDEX`
Outside of `./lib/opsem/ptrecon.ml` change each occurence of `PTRECON` to `Ptrecon.PTRECON`
Outside of `./lib/opsem/subtree.ml` change each occurence of `MEMOTABLE` to `Subtree.MEMOTABLE`
Outside of `./lib/opsem/sw_subtree.ml` change each occurence of `MEMOTABLE` to `Sw_subtree.MEMOTABLE`
Outside of `./lib/opsem/table_param.ml` change each occurence of `TABLEPARAM` to `Table_param.TABLEPARAM`
Outside of `./lib/opsem/tabled.ml` change each occurence of `TABLED` to `Tabled.TABLED`
Outside of `./lib/opsem/trace.ml` change each occurence of `TRACE` to `Trace.TRACE`
Outside of `./lib/order/order.ml` change each occurence of `ORDER` to `Order.ORDER`
Outside of `./lib/paths/origins.ml` change each occurence of `ORIGINS` to `Origins.ORIGINS`
Outside of `./lib/paths/paths.ml` change each occurence of `PATHS` to `Paths.PATHS`
Outside of `./lib/print/clause_print.ml` change each occurence of `CLAUSEPRINT` to `Clause_print.CLAUSEPRINT`
Outside of `./lib/print/print.ml` change each occurence of `PRINT` to `Print.PRINT`
Outside of `./lib/print/print_omdoc.ml` change each occurence of `PRINT_OMDOC` to `Print_omdoc.PRINT_OMDOC`
Outside of `./lib/print/print_twega.ml` change each occurence of `PRINT_TWEGA` to `Print_twega.PRINT_TWEGA`
Outside of `./lib/print/print_xml.ml` change each occurence of `PRINT_XML` to `Print_xml.PRINT_XML`
Outside of `./lib/print/symbol.ml` change each occurence of `SYMBOL` to `Symbol.SYMBOL`
Outside of `./lib/print/traverse.ml` change each occurence of `TRAVERSER` to `Traverse.TRAVERSER`
Outside of `./lib/print/traverse.ml` change each occurence of `TRAVERSE` to `Traverse.TRAVERSE`
Outside of `./lib/prover/data.ml` change each occurence of `DATA` to `Data.DATA`
Outside of `./lib/prover/elim.ml` change each occurence of `ELIM` to `Elim.ELIM`
Outside of `./lib/prover/fill.ml` change each occurence of `FILL` to `Fill.FILL`
Outside of `./lib/prover/fixedpoint.ml` change each occurence of `FIXEDPOINT` to `Fixedpoint.FIXEDPOINT`
Outside of `./lib/prover/interactive.ml` change each occurence of `INTERACTIVE` to `Interactive.INTERACTIVE`
Outside of `./lib/prover/introduce.ml` change each occurence of `INTRODUCE` to `Introduce.INTRODUCE`
Outside of `./lib/prover/recurse.ml` change each occurence of `RECURSE` to `Recurse.RECURSE`
Outside of `./lib/prover/search.ml` change each occurence of `SEARCH` to `Search.SEARCH`
Outside of `./lib/prover/split.ml` change each occurence of `SPLIT` to `Split.SPLIT`
Outside of `./lib/prover/state.ml` change each occurence of `STATE` to `State.STATE`
Outside of `./lib/prover/stateprint.ml` change each occurence of `STATEPRINT` to `Stateprint.STATEPRINT`
Outside of `./lib/prover/weaken.ml` change each occurence of `WEAKEN` to `Weaken.WEAKEN`
Outside of `./lib/server/server.ml` change each occurence of `SERVER` to `Server.SERVER`
Outside of `./lib/server/sigint.ml` change each occurence of `SIGINT` to `Sigint.SIGINT`
Outside of `./lib/solvers/cs.ml` change each occurence of `CS` to `Cs.CS`
Outside of `./lib/solvers/cs_eq_field.ml` change each occurence of `CS_EQ_FIELD` to `Cs_eq_field.CS_EQ_FIELD`
Outside of `./lib/solvers/cs_eq_integers.ml` change each occurence of `CS_EQ_INTEGERS` to `Cs_eq_integers.CS_EQ_INTEGERS`
Outside of `./lib/solvers/cs_manager.ml` change each occurence of `CS_MANAGER` to `Cs_manager.CS_MANAGER`
Outside of `./lib/solvers/solvers.ml` change each occurence of `CS_INSTALLER` to `Solvers.CS_INSTALLER`
Outside of `./lib/stream/stream.ml` change each occurence of `BASIC_STREAM` to `Stream.BASIC_STREAM`
Outside of `./lib/stream/stream.ml` change each occurence of `STREAM` to `Stream.STREAM`
Outside of `./lib/style/style.ml` change each occurence of `STYLECHECK` to `Style.STYLECHECK`
Outside of `./lib/subordinate/intset.ml` change each occurence of `INTSET` to `Intset.INTSET`
Outside of `./lib/subordinate/subordinate.ml` change each occurence of `SUBORDINATE` to `Subordinate.SUBORDINATE`
Outside of `./lib/table/hash.ml` change each occurence of `HASHABLE` to `Hash.HASHABLE`
Outside of `./lib/table/hash.ml` change each occurence of `STRING_HASH` to `Hash.STRING_HASH`
Outside of `./lib/table/queue.ml` change each occurence of `QUEUE` to `Queue.QUEUE`
Outside of `./lib/table/red_black_set.ml` change each occurence of `RBSET` to `Red_black_set.RBSET`
Outside of `./lib/table/ring.ml` change each occurence of `RING` to `Ring.RING`
Outside of `./lib/table/sparse_array.ml` change each occurence of `SPARSE_ARRAY` to `Sparse_array.SPARSE_ARRAY`
Outside of `./lib/table/sparse_array2.ml` change each occurence of `SPARSE_ARRAY2` to `Sparse_array2.SPARSE_ARRAY2`
Outside of `./lib/table/string_hash.ml` change each occurence of `STRING_HASH` to `String_hash.STRING_HASH`
Outside of `./lib/table/table.ml` change each occurence of `TABLE` to `Table.TABLE`
Outside of `./lib/tabling/tabledsyn.ml` change each occurence of `TABLEDSYN` to `Tabledsyn.TABLEDSYN`
Outside of `./lib/terminate/checking.ml` change each occurence of `CHECKING` to `Checking.CHECKING`
Outside of `./lib/terminate/reduces.ml` change each occurence of `REDUCES` to `Reduces.REDUCES`
Outside of `./lib/thm/thm.ml` change each occurence of `THM` to `Thm.THM`
Outside of `./lib/thm/thmprint.ml` change each occurence of `THMPRINT` to `Thmprint.THMPRINT`
Outside of `./lib/thm/thmsyn.ml` change each occurence of `THMSYN` to `Thmsyn.THMSYN`
Outside of `./lib/timing/timers.ml` change each occurence of `TIMERS` to `Timers.TIMERS`
Outside of `./lib/timing/timing.ml` change each occurence of `TIMING` to `Timing.TIMING`
Outside of `./lib/tomega/abstract.ml` change each occurence of `TOMEGAABSTRACT` to `Abstract.TOMEGAABSTRACT`
Outside of `./lib/tomega/converter.ml` change each occurence of `CONVERTER` to `Converter.CONVERTER`
Outside of `./lib/tomega/coverage.ml` change each occurence of `TOMEGACOVERAGE` to `Coverage.TOMEGACOVERAGE`
Outside of `./lib/tomega/normalize.ml` change each occurence of `NORMALIZE` to `Normalize.NORMALIZE`
Outside of `./lib/tomega/opsem.ml` change each occurence of `OPSEM` to `Opsem.OPSEM`
Outside of `./lib/tomega/redundant.ml` change each occurence of `REDUNDANT` to `Redundant.REDUNDANT`
Outside of `./lib/tomega/tomeganames.ml` change each occurence of `TOMEGANAMES` to `Tomeganames.TOMEGANAMES`
Outside of `./lib/tomega/tomegaprint.ml` change each occurence of `TOMEGAPRINT` to `Tomegaprint.TOMEGAPRINT`
Outside of `./lib/tomega/typecheck.ml` change each occurence of `TOMEGATYPECHECK` to `Typecheck.TOMEGATYPECHECK`
Outside of `./lib/tomega/unify.ml` change each occurence of `TOMEGAUNIFY` to `Unify.TOMEGAUNIFY`
Outside of `./lib/trail/trail.ml` change each occurence of `TRAIL` to `Trail.TRAIL`
Outside of `./lib/typecheck/strict.ml` change each occurence of `STRICT` to `Strict.STRICT`
Outside of `./lib/typecheck/typecheck.ml` change each occurence of `TYPECHECK` to `Typecheck.TYPECHECK`
Outside of `./lib/unique/unique.ml` change each occurence of `UNIQUE` to `Unique.UNIQUE`
Outside of `./lib/worldcheck/worldify.ml` change each occurence of `WORLDIFY` to `Worldify.WORLDIFY`
Outside of `./lib/worldcheck/worldsyn.ml` change each occurence of `WORLDSYN` to `Worldsyn.WORLDSYN`
