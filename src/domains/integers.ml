Integers Integer INTEGER   INTEGERS  struct open Integer let  inlet  inlet rec solve_gcd(m,  , n) let let rec solve'(m,  , n) let let  inlet  in in if (r = zero) then (zero,  , one) else let let  in in (y,  , x - q * y)let  inlet  inlet  inlet  in in if (am > an) then (fun (x,  , y) -> (sm * x,  , sn * y)) (solve' (am,  , an)) else (fun (x,  , y) -> (sm * y,  , sn * x)) (solve' (an,  , am))let rec gcd(m,  , n) let let  in in m * x + n * ylet rec lcm(m,  , n) quot (m * n,  , gcd (m,  , n))let rec fromString(str) let let rec check(chars as (c :: chars')) if (c = '~') then (all isDigit chars') else (all isDigit chars) checknil false in if check (explode str) then fromString str else nONEend