FgnOpnTable  Arg    Result    FGN_OPN   Arg Arg   Result Result  struct type Csidtype Reptype Argtype Resulttype Functype Tablelet rec initializeTabletbl let exception let  inlet rec unimplementedcsid fun _ -> raise ((cSfunNotInstalled csid)) in tabulate (maxCSid + 1,  , unimplemented)let  inlet rec install(csid,  , f) update (table,  , csid,  , f)let rec apply(csid,  , rep) sub (table,  , csid) repend