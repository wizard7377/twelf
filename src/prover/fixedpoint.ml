FixedPoint  State' STATE     FIXEDPOINT  struct (*! structure IntSyn = IntSyn' !*)
(*! structure Tomega = Tomega' !*)
module module module module exception type Operator(* expand S = S'

       Invariant:
       If   S = (Psi |>  F)
       and  F does not start with an all quantifier
       then S' = (Psi, xx :: F |> F)
    *)
let rec expand(focus (eVar (psi,  , r,  , f,  , _,  , tCs,  , _),  , w),  , o) let (*        val D = T.PDec (SOME "IH" , F, SOME O, SOME O) *)
let  inlet  inlet  in in (r,  , rec (d,  , x))(* apply O = S

       Invariant:
       O = S
    *)
let rec apply(r,  , p) (r := sOME p)(* should be trailed -cs Thu Apr 22 11:20:32 2004 *)
(* menu O = s

       Invariant:
       s = "Apply universal introduction rules"
    *)
let rec menu_ "Recursion introduction"exception type Operatorlet  inlet  inlet  inend