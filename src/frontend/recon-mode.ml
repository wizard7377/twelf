ReconMode  Global GLOBAL    Whnf WHNF    Names NAMES    ModePrint MODEPRINT    ModeDec MODEDEC    ReconTerm' RECON_TERM     RECON_MODE  struct (*! structure ModeSyn = ModeSyn' !*)
module (*! structure Paths = Paths' !*)
exception let rec error(r,  , msg) raise (error (wrap (r,  , msg)))module module module module type Modelet rec plusr (plus,  , r)let rec starr (star,  , r)let rec minusr (minus,  , r)let rec minus1r (minus1,  , r)type Modedecmodule (* structure Short *)
module (* structure Full *)
let rec modeToMode(m,  , r) (m,  , r)type Modelet  inlet  inlet  inlet  intype Modedecmodule module let  in(* local ... *)
end